#!/bin/bash

# Aliases
alias gco='git co'
alias gci='git ci'
alias grb='git rb'

alias gip='git pull'
alias gic='git cherry-pick'

alias sbl='sublime'

alias check='phpcs -v --standard="$HOME/.phpcs/Fork" --extensions=php --report=full'
alias cc='cd tools && ./remove_cache && cd ..'
